import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};
module.exports = {
  images: {
    domains: [
      'via.placeholder.com',  
      'videoplayervidext.s3.us-east-1.amazonaws.com',
    ],
  },
};


export default nextConfig;
